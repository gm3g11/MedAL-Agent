# Stage 0a + 0b — SMOKE / INTEGRATION results (NOT a scientific AL benchmark)

Date 2026-06-13. Env: A40, torch 2.4.1+cu121.

> **These are smoke/integration results only.** Legacy smoke profile (img128,
> pool_cap=32, budget [16,32] = init→next, 15 iters). DSC values are meaningless as
> method comparisons (32 random slices, no convergence). The goal is to prove the
> pipeline runs end-to-end and the logging/determinism/leakage guarantees hold.

## Verdict: PASS — both stages green. Ready to build toward Stage 1 (still needs B2 adaptive resolution + the native-split decision).

## Runs

- **Stage 0a:** {busi, kvasir_seg, msd07_pancreas} × {P0,P1,P4,P8,P9} (15 cells) + all
  P0–P9 on busi (10 cells) + busi/P1 determinism replay. SAM-H for P7/P8.
- **Stage 0b:** {mmwhs (C=8), btcv_synapse (C=14), ext_abdoment1k (C=6)} × {P0,P1,P4,P8,P9}
  (15 cells) + mmwhs/P1 determinism replay. SAM-H.
- **Total: 40 cells, 40 OK, 0 failures, 0 `.fail.txt`.**
- Output: `runs/stage0_v2/{0a,0b,0a_allmethods_busi,0a_replay,0b_replay}/`.

## Checklist (your §"For both stages, confirm")

| item | result | evidence |
|---|---|---|
| forward/backward/optimizer step | ✅ | all 40 cells trained to completion, finite loss |
| loss accepts masks | ✅ | DiceCE ran on C∈{2,3,6,8,14} with no class-index error |
| DSC computes | ✅ | `dsc_per_class` length == num_classes for every dataset (2/3/6/8/14) |
| HD95 policy works or skipped w/ warning | ⚠️ skipped | smoke profile `compute_surface_metrics_at_final=False` → HD95/ASD **intentionally skipped**; computed first/mid/final in Stage 1 (B5). Documented. |
| selected IDs saved | ✅ | `selected_ids` in every JSONL; all unique (`sel_uniq=True`) |
| candidate scores saved | ✅ | `candidate_scores/*.json` sidecars exist for every selection round |
| checkpoint hash saved | ✅ | `ckpt_hash` (sha256) per round |
| deterministic replay | ✅ | busi/P1 & mmwhs/P1 re-runs → **identical** `selected_ids` + `ckpt_hash` |
| no unlabeled-mask access | ✅ | firewall (`test_firewall_no_unlabeled_pool_mask_access`, 133 tests green) |
| no val/test leakage | ✅ | case-disjoint splits: mmwhs 40/5/5, btcv 21/3/3, ext_abdoment1k 714/89/89 cases |
| runtime + memory logged | ✅ | `runtime_sec={train,eval,select}` + **`gpu_mem_mb`** per round (71 MB light; ~250 MB for P9's accuracy-predictor) |

## Notes (smoke artifacts, not bugs)

- busi/kvasir DSCs identical across methods: pool_cap=32 + budget [16,32] → round 1 trains
  on the whole 32-slice pool → all methods converge. Disappears with real budgets.
- msd07_pancreas DSC_fg=0.000 and the multiclass 0b DSCs ≈0: 32 random slices are mostly
  background under smoke (no fg-stratification); the pilot/bench profile fixes this.
- ext_abdoment1k (241k slices) bridged + trained fine; care_leftatrium_2026 needed the
  no-mask-slice filter (1189 dropped) — fixed in the bridge.

## Multiclass integration confirmed

The bridged medal_agent datasets exercised the full multiclass path through the runner:
btcv_synapse C=14, mmwhs C=8, ext_abdoment1k C=6 — loss, DSC, and selection all correct.
This is the integration gate; the formal benchmark (adaptive 512, pool-dependent budgets,
native splits honored, full-sup baseline, HD95) is Stage 1 and still gated on B2 + the
native-split decision (F1).
